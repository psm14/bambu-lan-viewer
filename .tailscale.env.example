# Copy this file to .tailscale.env and replace the placeholder values.
# Auth key docs: https://tailscale.com/kb/1085/auth-keys/
TS_AUTHKEY=tskey-auth-REPLACE_ME

# Optional: stable hostname for this node inside your tailnet.
TS_HOSTNAME=bambu-lan-viewer

# Optional: extra tailscale up flags.
# Example: --advertise-tags=tag:homelab
TS_EXTRA_ARGS=
